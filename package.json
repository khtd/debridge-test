{
  "name": "debridge-test",
  "version": "1.0.0",
  "description": "test assignment for deBridge by Sonya Agranov",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "clean": "node scripts/clean.mjs",
    "migrate:dev": "npm run migrate:dev --workspace @debridge-test/db",
    "migrate:latest": "npm run migrate:latest --workspace @debridge-test/db",
    "migrate:down": "npm run migrate:down --workspace @debridge-test/db",
    "gen:db-types": "npm run gen:db-types --workspace @debridge-test/db"
  },
  "author": "sonya agranov",
  "license": "ISC",
  "dependencies": {
    "glob": "^13.0.0",
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "ts-node": "^10.9.2"
  }
}
